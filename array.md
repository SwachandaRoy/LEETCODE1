Deletion: del arr[m:n]   deletes elements from m to n  </br>
Array input single line define: arr=list(map(int, input().split()))
Sorting a slice in place:   nums[lpeak:] = sorted(nums[lpeak:])       assign it to itself
